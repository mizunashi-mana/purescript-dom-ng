{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "pulp build -- --strict",
    "repl": "pulp repl",
    "pkg-install": "bower install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "bower": "^1.8.2",
    "pulp": "^12.0.1",
    "purescript": "^0.11.6",
    "purescript-psa": "^0.5.1"
  }
}
